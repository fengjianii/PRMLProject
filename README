MEOW(Mid-term Equities Offline Workshop)

Requirements:
2. Python >= 3.8

To run the example code:
    -- cd <project directory>
    -- python3 meow.py

Optional C module:
    -- see AGENT_TRADING.md for prediction export and lightweight Trading Agent backtest usage.
