# Task A 改动说明 & 协同待办

> 更新时间：2026-06-02  
> 改动人：A（npu2023302664）  
> 涉及文件：`feat.py`、`mdl.py`

---

## 一、做了什么

### 1. feat.py：54 → 76 个特征

在 B 之前的 54 特征基础上，做了三件事：

**（1）修复了 shift 跨天 bug**

原始代码和 B 的代码里，`shift()` 和 `rolling()` 没有区分不同的交易时段。比如一只股票当天最后一分钟的数据会和下一天第一分钟的数据连在一起做 shift，导致特征计算错误。

我的改法：所有 `shift` / `rolling` / `diff` 操作都通过 `_per_session()` 按 `(symbol, date)` 分组执行，确保不会跨天。

**（2）修正了 cs_rank 的分组粒度**

B 的 `_cs_rank` 是按 `groupby("interval")` 排名的——这会把不同日期的同一时刻（比如 6 月 1 日 9:31 和 6 月 2 日 9:31）混在一起排名。修正为 `groupby(["date", "interval"])`，保证只比较同一时刻、同一天的所有股票。

**（3）新增了 22 个特征（P0 + P1）**

基于 B 在 `总结.md` 里提出的改进方向：

| 类别 | 数量 | 说明 | 举例 |
|------|------|------|------|
| **P0 滚动统计** | 14 个 | 对核心特征（ob_imb0, trade_imb 等）做 12 窗口的 mean/std/skew，以及 6 窗口的变化率 | `ob_imb0_roll_mean_12`、`trade_imb_change_6` |
| **P1 横截面** | 8 个 | 对收益率 / 波动率等做截面 rank，以及截面 rank 的变化 | `cs_rank_ret_3`、`cs_rank_ret_3_change` |

金融含义：
- 滚动统计捕捉特征的**时序动态**——盘口压力是在增强还是减弱、波动是在收敛还是发散
- 横截面特征捕捉股票的**相对强弱**——不是"它涨了多少"，而是"它在所有股票里排第几"

### 2. mdl.py：更新默认参数

GBT 的默认参数改成了 B 在 `tuning_output/best_params.json` 里调出来的最优值：

```python
learning_rate=0.0156
max_iter=1600
max_depth=5
min_samples_leaf=200
max_leaf_nodes=31
early_stopping=True, validation_fraction=0.1, n_iter_no_change=50
```

---

## 二、验证结果

**5 天训练 + 2 天评估（76 特征 + B 调优参数）：**

```
Pearson correlation = 0.0631
```

对比参考：

| 方案 | 训练天数 | Pearson |
|------|---------|---------|
| 原始 Ridge + 6 特征 | 123 天 | 0.029 |
| B 的 GBT + 54 特征 | 123 天 | **0.0635** |
| **新版 GBT + 76 特征** | **5 天** | **0.0631** ✨ |

只用 5 天就追平了 B 的 123 天结果。**用完整 123 天训练后，预期应在 0.07~0.09 区间。**

---

## 三、B 接下来要做的事

### 3.1 跑完整训练 + 评估

拉取最新代码后，直接跑：

```bash
py meow.py
```

这会用 76 个特征 + 调优参数，训练 6-11 月（123 天），评估 12 月（21 天）。

> 注意：`meow.py` 里的数据路径是 `./data/`，B 需要确认自己的数据目录，如果路径不同就改一下。

### 3.2 看特征重要性

训练完用 `feature_importances_` 看一下哪些特征贡献最大，把排名前 20 的贴出来。如果 A 加的一些 P0/P1 特征排名很低，后续可以考虑删掉。

### 3.3 如果 Pearson 不到 0.07

可能需要调整的点：
- 某些 P0/P1 特征可能是纯噪声，去掉试试
- GBT 参数需要基于新特征集重新微调（之前调参是在 54 特征上做的）
- 考虑 B 在总结里提的 P2（模型集成）或 P3（目标变换）

### 3.4 更新报告

B 负责的报告章节（模型方法 + 实验结果）需要更新：
- 最终模型换成新的 GBT + 76 特征
- 特征数量从 54 更新到 76
- 指标表用最新结果

---

## 四、C 需要知道的

- 最终特征集从 54 增加到 76，交互特征还是 5 个（用 cs_rank 版本计算的）
- 如果 C 打算让 Agent 生成新因子，建议基于 cs_rank 后的特征做（和模型输入一致）
- 交易 Agent 的输入 `predicted_fret12` 不变，还是 `eval.py` 输出的那一列

---

## 五、文件清单

| 文件 | 修改人 | 状态 |
|------|--------|------|
| `feat.py` | A | ✅ 76 特征，已推送 |
| `mdl.py` | A | ✅ 默认参数已更新，已推送 |
| `meow.py` | A | 无改动（数据路径已是 `./data/`） |
| `eval.py` | 原始 | 无改动 |
| `REPORT.md` | A | ⏳ 待 B 出结果后更新 |
| `GBT-final/` | B | B 原来的工作目录，保留不动 |

---

有问题直接在这个文件下面评论或者群里说。
